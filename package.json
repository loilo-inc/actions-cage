{
  "name": "actions-cage",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "fmt": "prettier --write '**/*.ts'",
    "fmtcheck": "prettier --check '**/*.ts'",
    "test": "vitest run",
    "build": "tsx tools/main.ts build",
    "clean": "rm -rf build",
    "testci": "vitest run --coverage --coverage.provider=v8 --coverage.reporter=json --coverage.reportsDirectory=coverage"
  },
  "dependencies": {
    "@actions/core": "^3",
    "@actions/exec": "^3",
    "@actions/github": "^9",
    "@actions/io": "^3",
    "@actions/tool-cache": "^4",
    "semver": "^7.7.4"
  },
  "devDependencies": {
    "@types/node": "^25",
    "@types/semver": "^7.5.8",
    "@vitest/coverage-v8": "^4.1.2",
    "esbuild": "^0.27.4",
    "prettier": "^3",
    "tsx": "^4.21.0",
    "typescript": "^6",
    "vitest": "^4.0.16"
  }
}
